spring:
  datasource:
    url: jdbc:mysql://localhost:3306/reservation_purchase_database
    username: local_user
    password: local_password
  jpa:
    properties:
      hibernate:
        show_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

  level:
    org:
      springframework:
        web: DEBUG
jwt:
  secret-key: reservation_purchase_2024_secret_key
  accessToken.expired-time-ms: 2592000000

file:
  local:
    upload:
      path: /Users/jeongjihun/dev/2024/01-reservation_purchase/reservation_purchase/db/profile
